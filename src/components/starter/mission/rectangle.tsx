import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="h-16 w-0.5 mb-10 mx-auto" style="background: linear-gradient(180deg, #5ACC8F 0%, #3B8EE0 50%, #B59AFF 100%);" />
  );
});
